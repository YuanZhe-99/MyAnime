# Version history

Release-by-release summary of MyAnime!!!!!. Useful for understanding *why* a behavior exists before
changing it — several entries record deliberate safety fixes that look like quirks otherwise.

## Repository caveat

The latest release tag before the documentation guide was added was `v0.6.7`, with `origin/master`
and `github/master` aligned there. Early history has one wrinkle: two root commits share the same
content after an early `git commit --amend`, and tag `v0.1.0` points to the later root. **Avoid
`git commit --amend` on root commits.**

## Releases

- `v0.1.0`: Initial anime tracker.
- `v0.1.1`: Store/full flavors, daily reminders, JST calendar note, macOS support, CI/CD, platform naming and icons.
- `v0.1.2`: Per-record three-way WebDAV merge, conflict resolution UI, sync base tracking.
- `v0.1.3`: UTC `modifiedAt` fixes and schedule validation fixes.
- `v0.2.0`: Share anime as image card with cover, info, QR code, and watermark.
- `v0.2.1`: Share card broadcast-progress improvements and app logo.
- `v0.2.2`: Higher-resolution share card, truncation indicator, add-anime validation, desktop preview dialog.
- `v0.3.0`: AniList source, `infoUrl`, share URL options.
- `v0.3.1`: Android share target opens in its own task.
- `v0.4.0`: Statistics page, management global search, quarter jump picker.
- `v0.4.1`: Navigate to detail after creation and return to the anime quarter.
- `v0.4.2`: Redesigned quarter picker and scrollable statistics trend chart.
- `v0.4.3`: Year-by-quarter grid picker, wider quarter range, sticky Y-axis, default scroll to latest stats.
- `v0.4.4`: Quarter jump fix, optional `firstAirDate`, Japanese title fallback, Other page.
- `v0.5.0`: Comprehensive i18n cleanup for day names, season names, notifications, sync conflicts, and search labels.
- `v0.5.1`: Share as `.myanimeitem` data file and file-open support on all platforms.
- `v0.5.2`: Import `.myanimeitem` from add menus, strip personal viewing data from export, better filenames, manual type override fix, installer metadata.
- `v0.5.3`: `airsInQuarter()` respects `manualType` and episode week offsets.
- `v0.5.4`: Markdown export option for LLM personalization.
- `v0.6.0`: Local HTTP API server, system tray service, desktop settings UI.
- `v0.6.1`: API server enable/disable toggle and configurable listen address.
- `v0.6.2`: Launch at startup, corrected unwatched endpoint, season filters for list/history.
- `v0.6.3`: API `nextEpisodeAirDate` and improved abandoned/not-yet-aired classification.
- `v0.6.4`: API list/history return `total`, `counts`, and `data`; abandoned classification fix.
- `v0.6.5`: Referenced-only image sync and detailed sync error/warning reporting.
- `v0.6.6`: Periodic auto-sync and forward-compatible JSON field preservation.
- `v0.6.7`: Kana quick reference module, l10n updates, remotes synchronized to both `origin` and `github`.
- `v0.7.0`: Optional anime rating system and statistics ranking view with rating filters and sorting.
- `v0.7.1`: Ranking filter UX improvements with reusable quarter/year pickers, quarter-range custom filters, corrected top selection state, and cover thumbnails.
- `v0.7.2`: Ranking button selected-state contrast, ranking image export/share, and release version metadata updates.
- `v0.7.3`: Statistics quick quarter/year pickers, quieter skipped-only calendar markers, richer unwatched episode counts, and reminder wording/count updates.
- `v0.7.4`: Configurable home calendar localization, week start, Japanese calendar layout, JST/local date-grid mode, and release version metadata updates.
- `v0.7.5`: Full-range statistics trend scrolling with focused quarter/year selection, all-scope quarter/year trend granularity, collapsed all-scope status lists, current-format home calendar button text, and release version metadata updates.
- `v0.8.0`: Local API status/rating/progress field refresh, `/anime/ranking` endpoint, shared viewing-status derivation, and release version metadata updates.
- `v1.0.0`: Pre-release audit hardening — WebDAV downloads distinguish 404 from errors so transient failures can never overwrite the remote or cascade into cross-device deletions, ETag `If-Match`/`If-None-Match` preconditions on data uploads with visible 412 errors, fresh local re-read before writing merged data, identical-content concurrent edits no longer raise conflicts, conflict-resolution upload failures are reported, content-aware per-day mobile reminder scheduling (empty days skipped, no duplicate in-app notification), IANA timezone resolution via `flutter_timezone`, Basic Auth enforced on loopback when API credentials are configured, allowlist-based ZIP import, removed unused `SCHEDULE_EXACT_ALARM` permission, forward-snapped episode air dates, and versions unified to `1.0.0+37` / MSIX `1.0.0.0` / installer `1.0.0`.
- `v1.0.1`: Statistics page share button (image or data file), summary image with horizontal bar chart (tracked/completed/dropped), multi-anime `.myanimeitem` bundle export/import (v2 format), import conflict resolution with keep-local/use-imported/merge options, settings duplicate check page with transitive grouping and merge, CI Flutter updated to `3.44.2`, and versions unified to `1.0.1+38` / MSIX `1.0.1.0` / installer `1.0.1`.
- `v1.0.2`: Statistics image sharing now supports summary status selection with filtered bar-chart counts, large-image warnings, optional row limits, summary first-air-date priority, ranking first-N limiting in current rank order, generation progress dialogs while cover images load, and versions unified to `1.0.2+39` / MSIX `1.0.2.0` / installer `1.0.2`.
- `v1.1.0`: WebDAV auto-sync failures and true sync conflicts are surfaced in Settings/WebDAV, background sync no longer silently resolves conflicts with LWW, manual conflict resolution clears the visible status on success, and versions are unified to `1.1.0+40` / MSIX `1.1.0.0` / installer `1.1.0`.
- `v1.1.1`: Statistics share now supports a TXT export of anime display names sorted in dictionary order, statistics/ranking image exports are split across multiple PNG pages when the single-page pixel height would exceed the platform texture cap (`_maxImageDimension = 16000`) so tall lists are no longer cut off, multi-page sharing uses Android `ACTION_SEND_MULTIPLE` plus a desktop multi-page preview with save-all, and versions are unified to `1.1.1+41` / MSIX `1.1.1.0` / installer `1.1.1`.
- `v1.1.2`: WebDAV uploads now use a remote `.lock` with a stable local client id and 150-second TTL, interrupted local uploads are detected on the next sync, and HTTP 412 upload races re-download remote data and re-run per-record merge before surfacing only true record conflicts; versions are unified to `1.1.2+42` / MSIX `1.1.2.0` / installer `1.1.2`.
- `v1.1.3`: WebDAV now acquires `.lock` before downloading and merging remote data, lowers the lock TTL to 60 seconds, and force-uploads complete merged/resolved JSON under the valid lock without data-file `If-Match`/`If-None-Match` retry loops; versions are unified to `1.1.3+43` / MSIX `1.1.3.0` / installer `1.1.3`.
- `v1.2.0`: WebDAV sync hardening and force transfers — remote image listing failures no longer masquerade as an empty directory (fixing repeated re-uploads of already-uploaded images), transient network errors and HTTP 5xx are retried with backoff, sync progress is published through `WebDAVService.progress` and shown as a progress bar with localized phase text, Force Upload / Force Download actions with confirmation dialogs were added to the WebDAV page, sync-written JSON is pretty-printed to match local saves, `AnimeStorage` data/config writes and sync base/lock writes are atomic, `notifySaved` is ignored before auto-sync starts, downloaded images trigger UI reloads, manual sync notifies reload listeners, WebDAV terminology was standardized across MyAnime/MyDay/MyDevice (`settingsWebDAVTest` renamed to `settingsWebDAVTestConnection`), the search cover-fetch error is localized, installer filenames derive from `AppVersion`, and versions are unified to `1.2.0+44` / MSIX `1.2.0.0` / installer `1.2.0`.
- `v1.2.1`: Foreground sync operations (manual sync, conflict finalize, force upload/download) hold a screen wake lock via `sync_wake_lock.dart` and `wakelock_plus`, released in `finally` on completion/failure/cancel/exception; dismissing a sync conflict dialog now aborts the resolution instead of silently uploading keep-local; backup retention gains a 3-day option; Japanese `statsTracked`/`statsWatching` values were un-swapped; backup/restore terminology was standardized across MyAnime/MyDay/MyDevice (`backupRestoreModules` says "Modules", ja `backupCreate` unified); remaining ASCII `...` in localization strings was converted to `…`; and versions are unified to `1.2.1+45` / MSIX `1.2.1.0` / installer `1.2.1`.
- `v1.2.2`: Backup overhaul — backup format v2 stores images once in a content-addressed `backups/blobs/` store shared by all backups with reference-counted GC (a blob is deleted only when no remaining backup references it; legacy inline-image bundles stay restorable), bundle writes are atomic and corrupt bundles are flagged in the history (restore disabled) without suppressing the daily auto-backup retry, restoring a backup now reloads open pages and reminders, disables WebDAV auto-sync when sync is configured and offers an explicit force upload of the restored data (preventing restored-old data from propagating deletions to other devices), restore validates module JSON and sanitizes image names before writing, Home/Manage/Stats pages register sync reload callbacks (fixing the documented-but-unwired UI reload after background sync), auto-backup also runs from the 15-minute periodic timer with a re-entrancy guard, backup failures show a snackbar, the restore module label is localized, backup UI text/CJK terminology was standardized across MyAnime/MyDay/MyDevice (zh uses 还原), unused test imports were removed, and versions are unified to `1.2.2+46` / MSIX `1.2.2.0` / installer `1.2.2`.
- `v1.2.3`: Sync/backup safety hardening and Android build modernization — restoring a backup now disables WebDAV auto-sync *before* the first file is written and re-enables it only when the restore failed without writing anything (`RestoreResult` with `ok`/`wroteAnything`/`missingImages`), missing v2 backup image blobs surface a localized `backupRestoreMissingImages` warning instead of being silently dropped, WebDAV data/image PUTs heartbeat-refresh the remote `.lock` every 20 seconds so transfers slower than the 60-second TTL can no longer race a second client into a lost update, GitHub Actions were bumped to checkout v7 / setup-java v5 / upload-artifact v7 / download-artifact v8 / cache v6 / gh-release v3 (removing the Node 20 deprecation warnings), Android moved to Gradle 9.3.1 + AGP 9.1.1 with the app-side Built-in Kotlin migration done under the `builtInKotlin=false` compat flags, `file_picker` is pinned to exactly `10.3.7`, and versions are unified to `1.2.3+47` / MSIX `1.2.3.0` / installer `1.2.3`.
- `v1.3.0`: Shared `myapps_data` package extraction — WebDAV sync, backup/restore, ZIP transfer, and auto-sync scheduling now come from a shared package embedded as a git submodule at `packages/myapps_data`, which MyAnime, MyDay, and MyDevice all build on; 2,038 lines of engine code left this repo and the services became thin facades, so all existing tests pass unmodified. Two intentional behavior changes, both adopting what the other two apps already did: `finalizePendingSync` re-downloads the data file before uploading a resolution (also guarding against uploading over a remote that became unreadable), and a ZIP containing a path-traversal entry is rejected outright instead of having that entry skipped and the rest imported. Versions unified to `1.3.0+48` / MSIX `1.3.0.0` / installer `1.3.0`.
- `v1.3.1`: Fixed the custom storage-path migration. Changing the storage folder previously moved only `anime_data.json`, stranding `images/`, `.sync_base/`, `backups/` (bundles and blobs), and `webdav_config.json` at the old location. The worst consequence was silent: without `.sync_base/` the next sync has no base snapshot, so records other devices had deleted read as "new locally" and were re-uploaded, resurrecting deletions across every device; losing `webdav_config.json` also stopped sync entirely, which disabled the image sync that would otherwise have repaired the missing covers. Migration now moves the **entire** storage folder except `storage_config.json` (which must stay put — it holds the path itself), so a data file added in a future release migrates with no code change. Existing destination files still win, and their source copies are now left in place rather than discarded. Shared logic lives in `myapps_data` `v1.0.1` as `migrateStorageContents`, covered by eight tests. Versions unified to `1.3.1+49` / MSIX `1.3.1.0` / installer `1.3.1`.
- `v1.3.2`: Home calendar layout and remembered view. The weekday header row was clipped on every platform — `home_page.dart` never set `TableCalendar.daysOfWeekHeight`, so the package's `16.0` default applied while a Material `bodySmall` label lays out at ~16 px normally and over 20 px at a large system font setting; `table_calendar` wraps each weekday cell in a `SizedBox` of exactly that height, and `RenderParagraph` clipped the excess, cutting the top and bottom strokes off 日月火水木金土 (most visible on square and landscape windows, where oversized cells made the squashed header obvious). The header height is now derived from the label style and `MediaQuery.textScaler`, the weekday labels use theme colours instead of `DaysOfWeekStyle`'s hardcoded greys (which were near-invisible in dark mode), the grid is capped at `homeCalendarMaxWidth` (560) and centred so wide windows stop stretching the day cells, and short viewports get proportionally shorter rows (52 down to 34) with tighter cell margins and smaller airing markers so a six-week month still fits. The sizing math lives in `calendar_preferences.dart` as pure functions (`homeCalendarDaysOfWeekHeight`, `homeCalendarRowHeight`) with unit tests plus a widget regression test asserting the weekday label box is never shorter than the text it holds. Separately, the selected calendar view (month / two weeks / week) moved from `_HomePageState` into `AppSettings` as `homeCalendarFormat`, persisted to `storage_config.json` — previously it reset to month on every app launch *and* on every bottom-nav tab switch, since the `go_router` shell rebuilds `HomePage`. Versions unified to `1.3.2+50` / MSIX `1.3.2.0` / installer `1.3.2`.
- `v1.3.3`: Local archive tracking. Each anime gains an optional `localArchive` record — whether a downloaded local copy is kept, its source medium (`BD`/`DVD`/`WEB`/`TV`/other) and resolution (`2160p`/`1080p`/`720p`/`480p`/other) as two independent axes so `BD 1080p` and `WEB 1080p` stay distinguishable, how many copies exist, and a free-text repository code or location (several codes can share the one field). It is edited in a collapsible section of the edit page modeled on the rating section, shown as a read-only card on the detail page, and filterable on the management page through a three-state AppBar filter (all / archived / not archived, where "not archived" folds together "never recorded" and "explicitly not kept", since the question is what still needs downloading). **WebDAV sync required no code change at all**: `anime_data.json` is merged whole-record by `mergeRecords<Anime>` keyed on `id` + `modifiedAt`, so teaching `toJson`/`fromJson` about the field was sufficient, and `data_modules.dart`, `sync_merge.dart`, and `webdav_service.dart` were left untouched. `AnimeLocalArchive.hasAnyData` gates serialization so an anime that never used the feature emits no `localArchive` key and serializes byte-identically to before — the WebDAV request goldens still pass unmodified, which is the proof the wire format is unchanged. Because the values name the user's own storage infrastructure rather than their viewing habits, the field is synced but never shared: it is stripped from `.myanimeitem` exports alongside `episodeStatuses`/`episodeWeekOffsets`, and never drawn into image cards (the three canvas renderers read named properties, so exclusion is the default). It *is* exposed as an additive key on the loopback, Basic-Auth-protected local HTTP API. Duplicate merge takes the record whole from the first source that has one rather than field-by-field, since mixing a `source` from one record with a `location` from another would describe a copy that does not exist. Enum labels live in a new shared `lib/features/anime/views/archive_labels.dart` rather than being duplicated per page, with `BD`/`1080p`-style technical tokens hardcoded and only "other" localized. Versions unified to `1.3.3+51` / MSIX `1.3.3.0` / installer `1.3.3`.
- `v1.4.0`: Search overhaul — richer metadata, a usable result list, and cross-language queries. **Pulling more:** the sources were already returning far more than the app read. AniList's GraphQL document had requested `airingSchedule` since v0.3.0 but the parser never touched it, so the broadcast weekday was *guessed* from `startDate.weekday` — wrong for any series whose premiere aired off its regular slot; it now comes from `nextAiringEpisode.airingAt` (falling back to the first schedule node, queried without `notYetAired` so finished shows still resolve), converted to Japan time. bangumi.tv's `air_weekday` and rating block were likewise sitting unread in every response. Jikan now contributes `titles[]`, studios, genres, duration, format, status, and score — and only trusts `broadcast.time` when `broadcast.timezone` says `Asia/Tokyo`, instead of labelling a non-Tokyo time as JST. Per-source caps went from 5 to 10. Late-night slots are filed under the previous day in `25:00` form, following the Japanese 04:00 programming-day boundary the app already assumes: a 01:00 Thursday airing becomes Wednesday `25:00`. This is load-bearing because `getEpisodeCalendarDate()` ignores `airTime` entirely and places an episode from `firstAirDate` + `airDayOfWeek` alone, so the weekday and the first-air date are shifted **together** — shifting only the weekday would leave the two disagreeing and the forward-snap would push episode 1 a whole week out. **Storing it:** a new `externalMeta` record (`AnimeExternalMeta` + `AnimeExternalRating`) holds alternate titles, romaji/English titles, format, status, duration, genres, studios, end date, and one rating entry per source. External scores are kept strictly apart from the user's own `AnimeRating` — nothing writes one into the other — and each rating remembers the `sourceUrl` it came from, which is what makes refreshing possible: a detail-page action re-fetches every remembered page by id (AniList `Media(id:)`, Jikan `/full`, bangumi `/subject/<id>`, all through the *same* mappers the search path uses) and folds the results in, taking each field only when the new fetch actually supplies it so refreshing against one source never erases another's contribution. Unlike `localArchive`, this is public information about the work, so it travels in `.myanimeitem` share files. WebDAV needed no change again — whole-record merge plus `hasAnyData`-gated serialization. **Two dead sources, found by testing against the real APIs.** bangumi.tv had been on the legacy `GET /search/subject/` endpoint since the first release; it now returns `502 Bad gateway` from Cloudflare for every request, and because each source method treats a failure as an empty result, bangumi.tv had silently dropped out of search entirely with no error surfaced anywhere. It moved to the v0 API (`POST /v0/search/subjects`, `GET /v0/subjects/{id}` — plural), which is alive and returns strictly more: `infobox` carries the broadcast weekday, alternate titles, studio, and end date, none of which the legacy search response had. Separately, filmarks.com moved its detail URLs from `/anime/<id>` to `/animes/<series>/<season>` and now renders results client-side, so the `/anime/` patterns unchanged since v0.1.0 matched nothing; the scraper was rewritten against the current markup, taking the title from the poster `alt`. The lesson is recorded in the source docs: a source can disappear without anyone noticing, so a sudden zero-result run means suspect the source before the query. **Cross-language search:** `searchAll` now runs two rounds. Round one sends each source the variant it indexes best (Simplified to bangumi.tv, Traditional to acgsecrets.hk, `Accept-Language: ja` to filmarks.com), replacing the old bangumi-only Simplified special case. Round two harvests up to three high-confidence titles (Japanese, romaji/English, Chinese) from round one and re-queries *only* the sources that came back empty — which is what finally lets a Chinese query reach filmarks.com, a Japanese-title-only site. One extra round, never recursive. Results are deduplicated and ranked by a public `relevance()` that scores every known title of a result, so ranking is language-neutral. **Result UI:** sort (relevance / first air date / episodes / source, with missing values always sinking rather than sorting as zero), filter (source chips, has-cover, has-air-date), group-by-source, and long-press — a row's title is clipped to one line, and long-pressing opens a sheet with every title as selectable, copyable text plus all fetched metadata; desktop gets a hover tooltip. The local API gained the new fields additively, an optional `language`, and a `limit` defaulting to 10. Six source-format parsers are exposed `@visibleForTesting` and covered by 18 new tests, since the HTTP calls are static and offer no injectable seam. The `functions/INDEX.md` totals were recomputed against the real source tree: they claimed 682/685 while the per-file rows summed to 615 and the tree actually held 622 `Purpose:` comments, and the corrected figures now carry a note about the remaining coverage gap. Versions unified to `1.4.0+52` / MSIX `1.4.0.0` / installer `1.4.0`.
- `v1.5.0`: Background metadata updates. Until now, keeping database info current was entirely manual — open a record, press "refresh database info", one anime at a time. A new `MetadataUpdateService` runs two queues while the app is open, split by **who owns the field**: `externalMeta` is a local copy of someone else's public data, so records that already know their source get it refreshed silently; `title`, `endEpisode`, `firstAirDate` and the rest belong to the user, so anything that would change one becomes a **proposal** reviewed from a badge on the management page — one field at a time, per record, per management page, or library-wide behind two confirmations. **The load-bearing decision is that background writes never touch `modifiedAt`.** `mergeRecords` decides whether a record changed purely by comparing `modifiedAt` against the sync base and never by comparing content, so bumping it in the background would resurrect records deleted on another device ("modified locally after remote deleted → keep") and raise conflict dialogs for edits nobody made; leaving it alone keeps `localChanged` false, which makes both impossible while the refreshed data still propagates and still uploads. The detail page's existing manual refresh chip carried the same hazard and was moved onto the same `AnimeStorage.patchExternalMeta` path. A trap found by the new tests: `Anime.copyWith` stamps `modifiedAt` with the current time whenever the argument is omitted rather than preserving it, so both call sites must pass the old value back in explicitly — the first version of `patchExternalMeta` did not, which would have defeated the entire design; a regression test now pins it. An episode-count mismatch is only reported when `startEpisode == 1`, because a split-cour entry tracked as episodes 13–24 legitimately has `totalEpisodes == 12` against a source reporting 24, and without that guard the feature would have raised false alarms on exactly the records a user curated most carefully. `diffCandidate` never proposes overwriting a non-empty user value, with `endEpisode` on a demonstrable mismatch as the single exception, which is what makes "update all" defensible; anything the confidence rules could not separate is filed as `needsManualPick` and excluded from every batch action. Rate limiting stays well under each source's published limits (5 s between refreshes, 15 s between searches) with a 1 h → 6 h → 24 h → 7 d backoff, and **being offline is deliberately not counted as a failure** so a subway ride cannot push a whole library into a seven-day backoff. New `connectivity_plus` dependency gates the work by link type: on by default on desktop, defaulting to "don't use cellular data" on Android and iOS — worded that way, and documented as a heuristic rather than a guarantee, because the plugin reports the transport and not whether it is metered (a phone hotspot still reports Wi-Fi). Downloaded candidates and attempt/backoff state live in a new device-local `metadata_updates.json` that is neither synced nor backed up — which needs no special handling, since both engines only touch files registered in `ModuleRegistry`. Cover prefetch is a separate, off-by-default switch, the only large data this cache would hold. Also removed the "From external databases — separate from your own rating" line under the external-ratings section: the card title, the section heading, and the per-source chip labels already carry that distinction, so the sentence restated in prose what the layout was already showing. Also fixed a pre-existing bug found in passing: `addOrUpdate` and `deleteAnime` dropped `AnimeData.extraJson`, so unknown top-level fields written by a newer build were discarded by any older build's edit, contrary to this repo's own forward-compatibility rule. Three more defects were found by driving the real app rather than by the test suite: every record was fetched **twice**, because a completed refresh sat in the unflushed write buffer and so still looked never-fetched to the queue on disk (the buffer batches writes; it must not make the queue forget what it just did); the English batch dialogs read "Apply all 1 updates?" and "This changes 1 records at once" — the counts are ints, so the ungrammatical form compiled and only surfaced at exactly one record, the most common batch size of all, now fixed with ICU plurals and pinned by locale tests; and a `needsManualPick` card showed "100% match" directly above "No match was clear enough", which is self-contradictory — the title really did score 100%, but a rival *work* scored nearly the same, so the percentage is now hidden unless a candidate is actually being offered. Test count 105 → 143. Versions unified to `1.5.0+53` / MSIX `1.5.0.0` / installer `1.5.0`.
- `v1.5.1`: An entrance to the update screen, a check the user can ask for, and progress on both. 1.5.0 shipped the background pipeline with only its passive half: the management page's badge button was gated on `_pendingUpdateCount > 0`, so with nothing pending the button did not exist — and the review screen behind it was the only place a check could be started. That left the users with nothing yet, including everyone whose network policy is `off`, with no way in at all. The action is now always offered in full builds and only the badge comes and goes, and the screen carries a **Check for updates** action in the app bar and again as the empty state's primary button. The check walks a snapshot queue from `buildScanQueue`, fixed at the start so the progress bar's denominator never moves and the loop is guaranteed to terminate. What it ignores and honours is the design: failure backoff and the 30-day rediscovery window are **ignored**, because "try again now" is what the button means and those are only "don't retry too soon" heuristics; dismissals are **honoured**, because re-proposing a refusal would make *Ignore* meaningless; cache freshness is **honoured**, because re-fetching still-fresh metadata across a 200-record library costs minutes and over a thousand requests to change nothing; and the `metadataAutoUpdate` policy is **ignored**, because that setting governs *unattended* traffic while this is one explicit tap the user can watch and stop — and when the policy is `off` this button is the only way to check. Being offline is still refused, since every item would fail and push the whole library into backoff for nothing; that required splitting `_isOffline` out of `_networkGate`, which short-circuits to `blocked` as soon as the policy is `off` — exactly the case the manual button has to work in. Manual gaps are tighter than the background ones because the user is watching (2 s between refreshes, 6 s between searches) and still bounded by the same arithmetic: a refresh is at most one request per host and a `searchAll` at most two, so ≤30 and ≤20 per minute per host against Jikan's 60 and AniList's 90. The background timer is cancelled for the duration — two workers on the same APIs would double the rate those gaps are sized for. An empty queue is reported as *everything is already up to date*, not *no updates found*: those are different answers, and only the first is true when nothing was checked because nothing needed checking. Cancelling lets the request in flight finish and keeps what was found. Second, **the search dialog no longer shows a bare spinner**. A search can legitimately take about half a minute — each source has its own 10–15 second timeout, and sources that come back empty are queried a second time with titles harvested from the first round — and over that stretch a lone spinner is indistinguishable from a hang. `searchAll` now takes an optional `onProgress` callback reporting an `AnimeSearchProgress` per round and per source answer, rendered as a determinate bar plus one chip per source. Each round keeps **its own denominator**, because round two re-queries only the empty sources and a single running total would grow mid-search and drive the bar backwards. A failed source counts as answered but stays distinguishable from one that found nothing, since those are different answers to someone deciding whether to search again — `_runRound` already swallowed per-source exceptions to keep one dead source from failing the whole search, so the report is emitted from that same handler. Third, a `needsManualPick` proposal gained a **Search manually** action, which opens the record's edit page with the search dialog already open and the title filled in, reusing that page's existing search-and-apply path rather than adding a second way to write a record from a search result. That required routing the review screen properly: it was pushed as an imperative `MaterialPageRoute`, and Flutter's `Navigator` places `pages`-based routes *below* imperatively pushed ones, so a `context.push` from inside it would have opened the edit page underneath the screen that launched it. It is now a `/metadata-updates` `GoRoute` carrying the page scope as `extra`. Test count 143 → 158. Versions unified to `1.5.1+54` / MSIX `1.5.1.0` / installer `1.5.1`.
- `v1.5.2`: A detail page that uses a wide screen, and a ranking that can use someone else's scores. **The detail page split is an aspect rule, not a width breakpoint, and the Galaxy Z Fold 8 is why.** The obvious implementation — split above some `sw600dp`-style threshold — is wrong on the exact device that motivated the work: the Fold 8 unfolds to a 4:3 *landscape* panel (2448 × 1848 px), so in portrait it is 3:4 and **narrower than the near-square Fold 7 it replaced** (1848 px against 1968 px), while the Fold 8 Ultra went the other way to 2256 px. One generation now spans roughly 672 to 954 logical pixels unfolded, and one device needs two different answers at one width: two panes in landscape, the original single column in portrait. So `useDetailTwoPane` tests three things — width ≥ 600, height ≥ 480, and width/height ≥ 0.82 — and the aspect test is the load-bearing one, sitting near the middle of the gap between the Fold 8's portrait 0.755 and the Fold 7 / Fold 8 Ultra's portrait 0.90. A 720 threshold, the value already used in `kana_page.dart`, would have failed the Fold 8, the Fold 5 and the Fold 6 outright; 840 would have left only the Ultra. The height floor is not redundant with the width floor: the aspect test alone admits *wide and short* viewports, and without it a folded Fold 8 cover screen rotated to landscape (~657 × 416 dp) and an ordinary phone in landscape (~915 × 412 dp) would both split into two cramped panes. Because the rule is about shape rather than device class, 4:3 and 16:10 tablets follow the Fold 8 exactly — two panes in landscape, single column in portrait. The decision reads `MediaQuery.sizeOf` rather than the `LayoutBuilder` constraints the panes are sized from, because measuring the `Scaffold` body subtracts the app bar and inflates the ratio, which would read the Fold 8 in portrait as 0.80 and leave almost no margin under the threshold. The page was refactored into four builders — cover, header, detail cards, episode list — so both layouts share one copy of every section, and the seam between the header and the detail cards *is* the pane boundary. Two cover-sizing limits were added only after looking at the rendered result rather than derived up front: a **half-pane cap**, because at 704 dp tall the flat header budget handed the cover 420 dp and a two-line title above three rows of chips then ran off the bottom of the pane; and a **width check**, because a 3:4 panel has so much vertical room relative to its width that height-only sizing produced a tall, thin cover. Second, **the Ranking view can now rank by the database score instead of your own.** External scores have been stored per source in `externalMeta.ratings` since 1.4.0 and nothing in Statistics ever read them. The load-bearing detail is that `scoreMax` is stored *per entry*, so anything comparing across sources must divide through — reading `score` raw would rank a bangumi.tv 7/10 above a MyAnimeList 85/100 — hence `normalizedScore` on the rating, `normalizedScoreFor` and `averageNormalizedScore` on the meta, with a `scoreMax <= 0` guard so a malformed entry cannot produce an infinity that sorts to the top. The average across sources is the default and a single source can be pinned; the average is the more robust default because which sources a record happens to carry varies per anime, while a pinned source silently drops every anime that lacks it. Switching source genuinely changes *which* anime appear, not just their order — your own rating drops everything unrated, the database drops everything never fetched — so the filter and the comparator were routed through one `_rankingScoreOf` seam rather than four call sites, three of which carried a `!` that silently depended on the filter having run first. The sub-score dropdown is replaced rather than disabled under the database source, since an external database reports one scalar and "sort by: visual" would be meaningless against it. `ShareService` needed no signature change: `sortLabel` was already a plain `String`. `/anime/ranking` gained the same dimension as additive, defaulted `scoreSource` and `externalSource` parameters, echoed back in `sort`, because `buildRankingSnapshotForQuery` exists specifically so the API and the UI cannot disagree about what a ranking means; `externalSource` without `scoreSource=external` is a 400 rather than being silently ignored. Test count 158 → 186, including a rendering suite that drives the real detail page at the actual logical-pixel geometry of each device named above — the Fold 8's two orientations are asserted as one device giving two answers — and a ranking suite seeded with one anime rated only by the user and one scored only by a database, so a switch that did nothing could not pass. Versions unified to `1.5.2+55` / MSIX `1.5.2.0` / installer `1.5.2`.
- `v1.5.3`: Long names you can finally read, lists that use a wide screen, and the split rule promoted out of the detail page. **The "when to split" rule was already correct; what was wrong was where it lived.** 1.5.2 derived it from the Galaxy Z Fold 8 — width ≥ 600, height ≥ 480, aspect ≥ 0.82, with the aspect test load-bearing because the Fold 8 unfolds to a 4:3 *landscape* panel and so must split in landscape and stay single-column in portrait — but it lived inside `detail_layout.dart` under detail-page names and was documented as a detail-page rule. It is now `canSplitLayout` in `shared/utils/adaptive_layout.dart`, with `useDetailTwoPane` as a one-line delegate the tests assert still agrees, and it has its own cross-cutting page at `doc/en-us/adaptive-layout.md` and a row in the AGENTS.md reading table. Once a window may split, the three data-browsing lists fill it: as many columns of at least 320 dp as fit, capped at four, laid out left to right then top to bottom. The column count follows Google's feed guidance — an adaptive minimum item width rather than a hardcoded count per breakpoint — while the gate keeps the aspect test, which is a **deliberate divergence** from Google's "use width, not aspect ratio" advice and is recorded as one: width alone cannot give the Fold 8 two different answers at one width. Each of the three modules remembers its own count in `storage_config.json`, defaulting to auto and clamped rather than rewritten when the window shrinks, so a count set on a desktop survives a fold and comes back on unfolding; the manifest already declared `screenLayout|screenSize|smallestScreenSize|density`, so that transition needs no lifecycle work at all. Management's swipe-to-edit and swipe-to-delete survive unchanged at one column and are dropped above it, because a horizontal drag inside one narrow cell is ambiguous — the new long-press sheet carries both actions anyway. That sheet is the other half of the release: every row in all three tabs truncates the title to one line, so long-pressing (or right-clicking, since holding a mouse button is not a gesture anyone makes on Windows) now shows every stored title in full, selectable and free to wrap, above **Edit** and **Delete**, with delete reusing the shared `confirmDelete` and inheriting its five-minute "don't ask again" window. `kana_page.dart`'s own hardcoded 720 is **deliberately left alone** and documented as a known exception, because routing it through the aspect rule would change its tablet-portrait behaviour and nobody asked for that. Tests went from 186 to 216. Versions unified to 1.5.3+56 / MSIX 1.5.3.0 / installer 1.5.3.
- `v1.5.4`: Kana and Settings brought onto the shared layout rule, and navigation moved to the side of a wide window. **The rail is the change that touches everything else.** Above 600 logical pixels the shell's bottom `NavigationBar` becomes a side `NavigationRail` — Google's own recommendation, and the one case the split rule deliberately rejects: a phone in landscape at 915 x 412 is far too short to carry two panes but spends a fifth of its height on a bottom bar while 915 pixels of width sit unused. So `useNavigationRail` is a **second, width-only rule**, kept separate from `canSplitLayout` on purpose and documented as such. It centres its five destinations (`groupAlignment: 0`) rather than top-aligning them, since it has no leading menu button or FAB to sit under. Two consequences follow through the rest of the app: every capacity is now measured against `shellContentWidth`, which subtracts the rail's 81 pixels, and the 80 dp that scrolling pages reserved for the bottom bar drops to 16 when there is no bottom bar. That correction is real and visible — **a tablet in landscape now gets two anime-list columns where 1.5.3 gave three**, because a third of the remaining 943 would be 306 wide, under the 320 minimum. The count changed because the room did; the rule did not. Kana becomes two columns when the split rule allows *and* two tables of at least 330 dp fit — the tall gojuon and yoon tables left, the short dakuten table and the rule cards right, with the script switch beside the search field instead of above it. The second gate is what keeps the narrower unfolded foldables on one column with no breakpoint of their own, while the Fold 8 Ultra and Pixel 10 Pro Fold do fit, at about 58 px per cell, level with a phone. This also **resolves the known exception 1.5.3 recorded**: kana's hardcoded `constraints.maxWidth >= 720` is gone, routed through a generalized `columnCapacity`, and doing so *preserved* tablet-portrait behaviour rather than changing it — the rail leaves 655 dp of rule width, which the 720 literal would now fail and the shared arithmetic passes. There is no longer a single inline width breakpoint in `lib/`. Settings becomes a list-detail layout on any window the split rule allows: the first-level list stays on the left, and the second-level page it leads to — WebDAV sync, backup, duplicate check, privacy policy, license — fills the right instead of pushing full-screen, with the selected row highlighted and a placeholder until something is picked. **None of the five pages needed a change to become embeddable**, because the pane hosts them in a nested `Navigator` holding one route, which reports `canPop == false` so their app bars grow no back arrow. The open-source licenses page stays full-screen, deliberately: it contains its own master-detail and nesting it would put three panes on screen. Folding shut keeps the selection rather than clearing it, mirroring how the column preference is clamped rather than rewritten. The settings gate reuses `canSplitLayout` exactly, with no extra width threshold — an accepted trade, since a Z Fold 5 unfolded leaves 578 dp and its rows are cramped there, in exchange for one rule instead of two. Settings dropdowns now read flush right, button face and menu items both, so the label does not jump sideways as the menu opens. Also fixed in passing: opening Settings raised an unhandled async error wherever `launchAtStartup` had not been set up, which is any context but the real app entry point. Tests went from 216 to 244. Versions unified to 1.5.4+57 / MSIX 1.5.4.0 / installer 1.5.4.
