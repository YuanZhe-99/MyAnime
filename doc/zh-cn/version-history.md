# 版本历史

MyAnime!!!!! 的逐版本摘要。在改动一个行为之前理解它*为什么*存在很有用——多条记录记录的是刻意的安全修复，否则看起来像怪癖。

## 仓库注意事项

添加文档指南之前的最近发布标签是 `v0.6.7`，`origin/master` 和 `github/master` 在那里对齐。早期历史有一个褶皱：一次早期的 `git commit --amend` 之后，两个根提交共享相同内容，标签 `v0.1.0` 指向较晚的根。**避免对根提交使用 `git commit --amend`。**

## 版本

- `v0.1.0`：最初的动画跟踪器。
- `v0.1.1`：store/full 风味、每日提醒、JST 日历备注、macOS 支持、CI/CD、平台命名和图标。
- `v0.1.2`：逐记录三方 WebDAV 合并、冲突解决 UI、同步基线跟踪。
- `v0.1.3`：UTC `modifiedAt` 修复和日程校验修复。
- `v0.2.0`：以带封面、信息、二维码和水印的图像卡片分享动画。
- `v0.2.1`：分享卡片播出进度改进和应用 logo。
- `v0.2.2`：更高分辨率的分享卡片、截断指示器、新增动画校验、桌面预览对话框。
- `v0.3.0`：AniList 来源、`infoUrl`、分享 URL 选项。
- `v0.3.1`：Android 分享目标在自己的任务中打开。
- `v0.4.0`：统计页、管理全局搜索、季度跳转选择器。
- `v0.4.1`：创建后导航到详情并返回动画季度。
- `v0.4.2`：重新设计的季度选择器和可滚动统计趋势图。
- `v0.4.3`：按季度的年网格选择器、更宽的季度范围、粘性 Y 轴、默认滚动到最新统计。
- `v0.4.4`：季度跳转修复、可选的 `firstAirDate`、日文标题回退、其他页。
- `v0.5.0`：对星期名、季名、通知、同步冲突和搜索标签的全面 i18n 清理。
- `v0.5.1`：分享为 `.myanimeitem` 数据文件，并支持所有平台的文件打开。
- `v0.5.2`：从新增菜单导入 `.myanimeitem`、导出剥离个人观看数据、更好的文件名、手动类型覆盖修复、安装包元数据。
- `v0.5.3`：`airsInQuarter()` 尊重 `manualType` 和剧集周偏移。
- `v0.5.4`：面向 LLM 个性化的 Markdown 导出选项。
- `v0.6.0`：本地 HTTP API 服务器、系统托盘服务、桌面设置 UI。
- `v0.6.1`：API 服务器启用/禁用开关和可配置监听地址。
- `v0.6.2`：开机自启、修正的未观看端点、list/history 的季过滤器。
- `v0.6.3`：API `nextEpisodeAirDate` 和改进的弃看/未播出分类。
- `v0.6.4`：API list/history 返回 `total`、`counts` 和 `data`；弃看分类修复。
- `v0.6.5`：仅引用图像同步和详细的同步错误/警告报告。
- `v0.6.6`：周期自动同步和向前兼容的 JSON 字段保留。
- `v0.6.7`：假名速查模块、l10n 更新、同步到 `origin` 和 `github` 两个远程。
- `v0.7.0`：可选的动画评分系统和带评分过滤器与排序的统计排名视图。
- `v0.7.1`：排名过滤器 UX 改进，带可复用的季度/年选择器、季度范围自定义过滤器、修正的顶部选择状态和封面缩略图。
- `v0.7.2`：排名按钮选中态对比度、排名图像导出/分享，以及发布版本元数据更新。
- `v0.7.3`：统计快速季度/年选择器、更安静的仅跳过日历标记、更丰富的未观看剧集计数，以及提醒措辞/计数更新。
- `v0.7.4`：可配置的主页日历本地化、周起始、日式日历布局、JST/本地日期网格模式，以及发布版本元数据更新。
- `v0.7.5`：带聚焦季度/年选择的全范围统计趋势滚动、全范围季度/年趋势粒度、折叠的全范围状态列表、当前格式的主页日历按钮文本，以及发布版本元数据更新。
- `v0.8.0`：本地 API 状态/评分/进度字段刷新、`/anime/ranking` 端点、共享观看状态派生，以及发布版本元数据更新。
- `v1.0.0`：发布前审计加固——WebDAV 下载区分 404 与错误，使瞬态失败永远不可能覆盖远程或级联成跨设备删除；数据上传上的 ETag `If-Match`/`If-None-Match` 前置条件，带可见的 412 错误；写合并数据前新鲜的本地重读；相同内容的并发编辑不再产生冲突；冲突解决上传失败被报告；内容感知的按天移动提醒调度（空天跳过、无重复应用内通知）；通过 `flutter_timezone` 解析 IANA 时区；配置了 API 凭据时在回环上强制 Basic Auth；基于允许列表的 ZIP 导入；移除未使用的 `SCHEDULE_EXACT_ALARM` 权限；前向吸附的剧集播出日期；版本统一为 `1.0.0+37` / MSIX `1.0.0.0` / 安装包 `1.0.0`。
- `v1.0.1`：统计页分享按钮（图像或数据文件）、带水平条形图（已看/已完成/弃看）的摘要图像、多动画 `.myanimeitem` 捆绑导出/导入（v2 格式）、带保留本地/使用导入/合并选项的导入冲突解决、设置重复检查页（传递性分组和合并）、CI Flutter 更新到 `3.44.2`，版本统一为 `1.0.1+38` / MSIX `1.0.1.0` / 安装包 `1.0.1`。
- `v1.0.2`：统计图像分享支持带过滤条形图计数的摘要状态选择、大图像警告、可选行数限制、摘要首播日期优先、按当前排名顺序的排名前 N 限制、封面图加载时的生成进度对话框，版本统一为 `1.0.2+39` / MSIX `1.0.2.0` / 安装包 `1.0.2`。
- `v1.1.0`：WebDAV 自动同步失败和真正的同步冲突在设置/WebDAV 中浮出，后台同步不再用 LWW 静默解决冲突，手动冲突解决成功后清除可见状态，版本统一为 `1.1.0+40` / MSIX `1.1.0.0` / 安装包 `1.1.0`。
- `v1.1.1`：统计分享支持按字典序排序的动画显示名 TXT 导出，统计/排名图像导出在单页像素高度会超过平台纹理上限（`_maxImageDimension = 16000`）时拆分为多个 PNG 页，使高列表不再被截断，多页分享使用 Android `ACTION_SEND_MULTIPLE` 外加带全部保存的桌面多页预览，版本统一为 `1.1.1+41` / MSIX `1.1.1.0` / 安装包 `1.1.1`。
- `v1.1.2`：WebDAV 上传改用带稳定本地客户端 id 和 150 秒 TTL 的远程 `.lock`，中断的本地上传在下一次同步被检测，HTTP 412 上传竞争重新下载远程数据并重新运行逐记录合并，然后才浮出真正的记录冲突；版本统一为 `1.1.2+42` / MSIX `1.1.2.0` / 安装包 `1.1.2`。
- `v1.1.3`：WebDAV 在下载和合并远程数据之前获取 `.lock`，把锁 TTL 降低到 60 秒，并在有效锁下强制上传完整的合并/已解决 JSON，不再有数据文件 `If-Match`/`If-None-Match` 重试循环；版本统一为 `1.1.3+43` / MSIX `1.1.3.0` / 安装包 `1.1.3`。
- `v1.2.0`：WebDAV 同步加固和强制传输——远程图像列目录失败不再伪装成空目录（修复已上传图像的重复重新上传），瞬态网络错误和 HTTP 5xx 带退避重试，同步进度通过 `WebDAVService.progress` 发布并显示为带本地化阶段文本的进度条，WebDAV 页面新增带确认对话框的强制上传/强制下载操作，同步写入的 JSON 美化打印以匹配本地保存，`AnimeStorage` 数据/配置写入和同步基线/锁写入原子化，`notifySaved` 在自动同步开始前被忽略，下载的图像触发 UI 重载，手动同步通知重载监听器，WebDAV 术语在 MyAnime/MyDay/MyDevice 间标准化（`settingsWebDAVTest` 改名为 `settingsWebDAVTestConnection`），搜索封面获取错误本地化，安装包文件名从 `AppVersion` 派生，版本统一为 `1.2.0+44` / MSIX `1.2.0.0` / 安装包 `1.2.0`。
- `v1.2.1`：前台同步操作（手动同步、冲突最终化、强制上传/下载）通过 `sync_wake_lock.dart` 和 `wakelock_plus` 持有屏幕唤醒锁，在完成/失败/取消/异常时的 `finally` 中释放；关闭同步冲突对话框现在中止解决过程，而不是静默上传保留本地；备份保留新增 3 天选项；日文 `statsTracked`/`statsWatching` 值被换回；备份/恢复术语在 MyAnime/MyDay/MyDevice 间标准化（`backupRestoreModules` 说"模块"，ja `backupCreate` 统一）；本地化字符串中剩余的 ASCII `...` 转换为 `…`；版本统一为 `1.2.1+45` / MSIX `1.2.1.0` / 安装包 `1.2.1`。
- `v1.2.2`：备份大改——备份格式 v2 把图像只存一次在内容寻址的 `backups/blobs/` 存储中，所有备份共享，带引用计数 GC（blob 只在没有剩余备份引用它时删除；旧内联图像捆绑仍可恢复），捆绑写入原子化且损坏捆绑在历史中标记（禁用恢复），且不抑制每日自动备份重试；恢复备份现在重载打开的页面和提醒，在同步已配置时禁用 WebDAV 自动同步，并提供对恢复数据的显式强制上传（防止恢复出的旧数据把删除传播到其他设备）；恢复在写入前校验模块 JSON 并净化图像名；主页/管理/统计页注册同步重载回调（修复已记录但未接线的后台同步后 UI 重载）；自动备份也从 15 分钟周期计时器运行，带可重入守卫；备份失败显示 snackbar；恢复模块标签本地化；备份 UI 文本/CJK 术语在 MyAnime/MyDay/MyDevice 间标准化（zh 用"还原"）；移除未使用的测试导入；版本统一为 `1.2.2+46` / MSIX `1.2.2.0` / 安装包 `1.2.2`。
- `v1.2.3`：同步/备份安全加固和 Android 构建现代化——恢复备份现在在第一个文件被写入*之前*禁用 WebDAV 自动同步，并且只在恢复失败且未写入任何内容时重新启用（带 `ok`/`wroteAnything`/`missingImages` 的 `RestoreResult`）；缺失的 v2 备份图像 blob 浮出本地化的 `backupRestoreMissingImages` 警告，而不是被静默丢弃；WebDAV 数据/图像 PUT 每 20 秒心跳刷新远程 `.lock`，使比 60 秒 TTL 更慢的传输不再可能把第二个客户端竞态成一次丢失更新；GitHub Actions 升级到 checkout v7 / setup-java v5 / upload-artifact v7 / download-artifact v8 / cache v6 / gh-release v3（移除 Node 20 弃用警告）；Android 迁移到 Gradle 9.3.1 + AGP 9.1.1，应用侧 Built-in Kotlin 迁移在 `builtInKotlin=false` 兼容标志下完成；`file_picker` 精确固定为 `10.3.7`；版本统一为 `1.2.3+47` / MSIX `1.2.3.0` / 安装包 `1.2.3`。
- `v1.3.0`：共享 `myapps_data` 包抽取——WebDAV 同步、备份/恢复、ZIP 传输和自动同步调度现在来自嵌入在 `packages/myapps_data` 的 git 子模块共享包，MyAnime、MyDay 和 MyDevice 都构建在它之上；2,038 行引擎代码离开本仓库，服务变成薄门面，因此所有既有测试无需修改全部通过。两个刻意的行为变更，都采纳了另外两个应用已有的做法：`finalizePendingSync` 在上传解决方案前重新下载数据文件（也防止上传覆盖一个变得不可读的远程），以及含路径穿越条目的 ZIP 被整体拒绝，而不是跳过该条目并导入其余部分。版本统一为 `1.3.0+48` / MSIX `1.3.0.0` / 安装包 `1.3.0`。
- `v1.3.1`：修复自定义存储路径迁移。更改存储文件夹以前只移动 `anime_data.json`，把 `images/`、`.sync_base/`、`backups/`（捆绑和 blob）和 `webdav_config.json` 遗留在旧位置。最坏的后果是静默的：没有 `.sync_base/`，下一次同步就没有基线快照，于是其他设备已删除的记录被读成"本地新增"并重新上传，在每台设备上复活删除；丢失 `webdav_config.json` 也使同步完全停止，从而禁用了本可修复缺失封面的图像同步。迁移现在移动**整个**存储文件夹，唯独不动 `storage_config.json`（它必须留在原地——它保存路径本身），因此未来版本新增的数据文件无需代码变更即可迁移。既有的目标文件仍然优先，它们的源副本现在留在原地而不是被丢弃。共享逻辑位于 `myapps_data` `v1.0.1` 的 `migrateStorageContents`，由八个测试覆盖。版本统一为 `1.3.1+49` / MSIX `1.3.1.0` / 安装包 `1.3.1`。
- `v1.3.2`：主页日历布局与记住视图。星期表头行在所有平台上都被裁切——`home_page.dart` 从未设置 `TableCalendar.daysOfWeekHeight`，于是套用了包的 `16.0` 默认值，而 Material `bodySmall` 标签常规下约排版到 16 px、在较大的系统字体设置下超过 20 px；`table_calendar` 把每个星期格包进正好那么高的 `SizedBox`，`RenderParagraph` 裁掉了溢出部分，切掉了 日月火水木金土 的上下笔画（在正方形和横向窗口上最明显，那里超大的日期格让被压扁的表头格外扎眼）。表头高度现在由标签样式和 `MediaQuery.textScaler` 推算，星期标签改用主题颜色而非 `DaysOfWeekStyle` 硬编码的灰色（那些灰色在深色模式下几乎不可见），网格宽度上限为 `homeCalendarMaxWidth`（560）并居中，使宽窗口不再拉伸日期格，短视口则按比例缩短行高（52 降到 34），并配合更紧的格边距和更小的播出标记，使六周的整月仍放得下。尺寸计算作为纯函数（`homeCalendarDaysOfWeekHeight`、`homeCalendarRowHeight`）落在 `calendar_preferences.dart`，配有单元测试，外加一个断言星期标签框绝不矮于其所容文本的控件回归测试。另外，所选日历视图（整月/两周/单周）从 `_HomePageState` 移入 `AppSettings` 的 `homeCalendarFormat`，并持久化到 `storage_config.json`——此前它在每次应用启动时*以及*每次底部导航标签切换时都会重置为整月，因为 `go_router` 外壳会重建 `HomePage`。版本统一为 `1.3.2+50` / MSIX `1.3.2.0` / 安装包 `1.3.2`。
- `v1.3.3`：本地存档记录。每部动画新增一条可选的 `localArchive` 记录——是否保管了本地下载资源、其片源介质（`BD`/`DVD`/`WEB`/`TV`/其他）与分辨率（`2160p`/`1080p`/`720p`/`480p`/其他）作为两个独立维度，使 `BD 1080p` 与 `WEB 1080p` 保持可区分、存了几份，以及一个自由文本的资料仓库代码或位置（多个代码可共用这一个字段）。它在编辑页一个仿照评分小节的可折叠小节中编辑，在详情页作为只读卡片展示，并可在管理页通过三态 AppBar 筛选器筛选（全部 / 已存档 / 未存档，其中"未存档"把"从未记录"和"明确未保留"合为一类，因为要问的问题是还有什么需要下载）。**WebDAV 同步完全无需代码改动**：`anime_data.json` 由 `mergeRecords<Anime>` 按 `id` + `modifiedAt` 整记录合并，因此只要让 `toJson`/`fromJson` 认识该字段就够了，`data_modules.dart`、`sync_merge.dart` 和 `webdav_service.dart` 均未改动。`AnimeLocalArchive.hasAnyData` 把控序列化，使从未使用过该功能的动画不产生 `localArchive` 键、序列化结果与之前逐字节一致——WebDAV 请求金样本未经修改仍然通过，这正是线格式未变的证明。因为这些值指明的是用户自己的存储基础设施而非观看习惯，该字段同步但从不分享：它与 `episodeStatuses`/`episodeWeekOffsets` 一起从 `.myanimeitem` 导出中剥离，也从不绘制进图片卡片（三个画布渲染器读取具名属性，因此排除是默认行为）。它**确实**作为追加键暴露在绑定回环地址、受 Basic Auth 保护的本地 HTTP API 上。重复项合并整体取用第一条拥有该记录的来源而非逐字段合并，因为把一条记录的 `source` 与另一条的 `location` 混在一起会描述出一份并不存在的拷贝。枚举标签放在新的共享文件 `lib/features/anime/views/archive_labels.dart` 中而不是逐页重复，其中 `BD`/`1080p` 之类技术术语硬编码，仅"其他"本地化。版本统一为 `1.3.3+51` / MSIX `1.3.3.0` / installer `1.3.3`。
- `v1.4.0`：搜索大改——更丰富的元数据、可用的结果列表，以及跨语言查询。**拉取更多：** 各来源本就返回了远多于应用所读取的内容。AniList 的 GraphQL 文档自 v0.3.0 起就请求了 `airingSchedule`，但解析代码从未读过它，于是放送星期是从 `startDate.weekday`*猜*出来的——凡首播不在常规时段的作品都会错；现在它来自 `nextAiringEpisode.airingAt`（回退到第一条排期节点，且查询不带 `notYetAired`，使已完结作品也能解析出结果），并转换为日本时间。bangumi.tv 的 `air_weekday` 与评分块同样一直静静躺在每个响应里没被读取。Jikan 现在贡献 `titles[]`、制作公司、类型标签、时长、作品形式、播出状态与评分——并且只在 `broadcast.timezone` 为 `Asia/Tokyo` 时才采信 `broadcast.time`，而不是把非东京时间当作日本时间标注。每源上限从 5 提高到 10。深夜时段按日本 04:00 编成日分界（本应用本就假定这一惯例）归入前一天，写成 `25:00` 形式：周四 01:00 放送即为周三 `25:00`。这一点是承重的，因为 `getEpisodeCalendarDate()` 完全不读 `airTime`，只用 `firstAirDate` + `airDayOfWeek` 定位某一集，所以星期与首播日期必须**一起**平移——只挪星期会让两者互相矛盾，向前对齐反而把第 1 集整整推迟一周。**保存下来：** 新增的 `externalMeta` 记录（`AnimeExternalMeta` + `AnimeExternalRating`）保存别名、罗马音/英文标题、作品形式、播出状态、时长、类型标签、制作公司、完结日期，以及每个来源一条的评分记录。外部评分与用户自己的 `AnimeRating` 严格分离——两者之间不存在任何写入路径——且每条评分都记住它的来源 `sourceUrl`，这正是刷新得以实现的前提：详情页的一个动作会按 id 重新抓取每个已记住的页面（AniList `Media(id:)`、Jikan `/full`、bangumi `/subject/<id>`，全部走**与搜索路径相同**的映射函数）并把结果折叠进来，每个字段只在新抓取确实提供时才采用，因此对某个来源刷新绝不会抹掉另一个来源的贡献。与 `localArchive` 不同，这是关于作品本身的公开信息，因此会随 `.myanimeitem` 分享文件一同流转。WebDAV 再次无需改动——整记录合并加上受 `hasAnyData` 把控的序列化。**两个失效的来源，靠实际调用真实 API 发现。** bangumi.tv 自首个版本起一直使用旧版 `GET /search/subject/` 端点；它现在对每个请求都返回 Cloudflare 的 `502 Bad gateway`，而由于每个来源方法都把失败当作空结果，bangumi.tv 实际上已完全从搜索中消失，且任何地方都不报错。它已迁移到 v0 API（`POST /v0/search/subjects`、`GET /v0/subjects/{id}` —— 复数路径），后者仍然可用且返回内容严格更多：`infobox` 携带放送星期、别名、制作公司与完结日期，这些旧版搜索响应一个都没有。另外，filmarks.com 把详情 URL 从 `/anime/<id>` 改成了 `/animes/<series>/<season>` 并改为客户端渲染，因此自 v0.1.0 未变的 `/anime/` 模式什么都匹配不到；抓取逻辑已针对当前标记重写，标题改取自海报的 `alt`。这一教训已写进来源文档：来源可以在无人察觉时消失，因此忽然返回零结果时应先怀疑来源而非查询。**跨语言检索：** `searchAll` 现在分两轮执行。第一轮把各来源索引得最好的变体发给它（简体给 bangumi.tv，繁体给 acgsecrets.hk，`Accept-Language: ja` 给 filmarks.com），取代了旧的仅针对 bangumi 的简体特判。第二轮从第一轮中收集最多三个高置信标题（日文、罗马音/英文、中文），并**只**对零结果的来源重新查询——这终于让中文查询能够触达只索引日文标题的 filmarks.com。额外轮次仅一轮，绝不递归。结果随后去重，并按公开的 `relevance()` 排序——它会对一条结果的每个已知标题打分，因此排序是语言中立的。**结果界面：** 排序（相关度 / 首播日期 / 集数 / 来源，缺值一律沉底而非按 0 参与排序）、过滤（来源 chip、有封面、有播出日期）、按来源分组，以及长按——列表行的标题被截断为一行，长按会打开一个面板，其中每个名称都是可选中、可复制的文本，并附上全部已抓取的元数据；桌面端另有悬停提示。本地 API 以纯增量方式新增了这些字段，外加可选的 `language` 与默认为 10 的 `limit`。六个来源格式解析函数以 `@visibleForTesting` 公开，并由 18 个新测试覆盖，因为 HTTP 调用是静态的、不提供可注入的接缝。`functions/INDEX.md` 的总计已对照真实源码树重算：它声称 682/685，而逐文件行合计仅 615、源码树实际有 622 个 `Purpose:` 注释；修正后的数字附有关于剩余覆盖缺口的说明。版本统一为 `1.4.0+52` / MSIX `1.4.0.0` / 安装包 `1.4.0`。
- `v1.5.0`：后台资料更新。此前保持资料库信息最新完全是手动的——打开一条记录、点「刷新资料库信息」，一次一部。新增的 `MetadataUpdateService` 在应用打开期间运行两条队列，划分依据是**字段属于谁**：`externalMeta` 是别人公开数据的本地副本，因此已经知道来源的记录会被静默刷新；`title`、`endEpisode`、`firstAirDate` 等属于用户，因此任何会改动它们的内容都变成一条**建议**，从管理页的角标进入审阅——可逐字段、逐记录、逐管理页，或经两道确认对全库应用。**承重的决定是后台写入绝不碰 `modifiedAt`。** `mergeRecords` 判断一条记录是否变化，只比较 `modifiedAt` 与同步基线，从不比较内容，因此在后台更新它会让在另一台设备上删除的记录复活（「远端删除后本地又改过 → 保留」），并为无人做出的编辑弹出冲突对话框；不动它会让 `localChanged` 恒为 false，使两者都不可能发生，而刷新到的数据仍然会传播、仍然会上传。详情页原有的手动刷新 chip 带有同样的隐患，已一并移到 `AnimeStorage.patchExternalMeta` 这条路径上。新测试揪出的一个陷阱：`Anime.copyWith` 在省略该参数时会把 `modifiedAt` 填成当前时间而不是保留原值，因此两个调用点都必须显式把旧值传回去——`patchExternalMeta` 的第一版没有这样做，那会让整个设计彻底失效；现已有回归测试锁定它。集数不一致只在 `startEpisode == 1` 时才报告，因为把后半季记成第 13–24 集的分割档记录，其 `totalEpisodes` 为 12 而来源报 24，这本身是合理的；少了这条守卫，本功能会专挑用户最用心整理的那些记录刷假警报。`diffCandidate` 绝不建议覆盖非空的用户值，唯一的例外是集数确实对不上时的 `endEpisode`，这正是「全部更新」得以成立的依据；置信度规则无法分辨的一律归档为 `needsManualPick` 并排除在所有批量操作之外。限流远低于各来源公布的限制（刷新间隔 5 秒、搜索间隔 15 秒），退避为 1 时 → 6 时 → 24 时 → 7 天，而且**离线被刻意不计为失败**，使一趟地铁不会把整个资料库推进七天退避。新增 `connectivity_plus` 依赖按链路类型门禁：桌面默认开启，Android 与 iOS 默认为「不使用蜂窝数据」——文案如此措辞，并在文档中被记为启发式而非保证，因为该插件报告的是传输方式而不是是否计费（手机热点同样报告 Wi-Fi）。已下载的候选与尝试/退避状态存放在新的设备本地文件 `metadata_updates.json` 中，既不同步也不备份——这不需要任何特殊处理，因为两个引擎都只碰 `ModuleRegistry` 中注册的文件。封面预下载是一个独立的、默认关闭的开关，它是这份缓存唯一可能容纳的大体积数据。同时删除了外部评分分区下「来自外部资料库，与你自己的评分相互独立」这句说明：卡片标题、分区标题与每个来源的 chip 标签本就承担了这个区分，这句话只是用文字重述了版面已经展示的内容。另外顺手修复了一个既有 bug：`addOrUpdate` 与 `deleteAnime` 会丢弃 `AnimeData.extraJson`，因此由更新版本写入的未知顶层字段会被任何旧版本的编辑丢掉，这与本仓库自己的向前兼容规则相悖。另有三个缺陷是靠驱动真实应用而非测试套件发现的：每条记录都被抓取了**两次**，因为已完成的刷新停留在尚未落盘的写入缓冲里，于是从磁盘看它仍然像是从未抓取过（缓冲是用来批量写盘的，不能让队列忘记自己刚做过什么）；英文批量对话框显示为「Apply all 1 updates?」与「This changes 1 records at once」——计数是 int，因此这种不合语法的写法能正常编译，且只在恰好一条记录时暴露，而那正是最常见的批量规模，现已用 ICU 复数修正并由本地化测试锁定；以及 `needsManualPick` 卡片在「No match was clear enough」正上方显示「100% match」，自相矛盾——标题确实得了 100 分，但另一部**作品**的分数几乎相同，因此现在只有在真正提议候选时才显示匹配度。测试数 105 → 143。版本统一为 `1.5.0+53` / MSIX `1.5.0.0` / 安装包 `1.5.0`。
- `v1.5.1`：更新页的入口、用户可以主动要求的检查，以及两处进度。1.5.0 交付的后台流水线只有被动的那一半：管理页的角标按钮以 `_pendingUpdateCount > 0` 为条件，因此没有待确认项时按钮根本不存在——而它背后的审阅页又是唯一能发起检查的地方。这让「还什么都没有」的那批用户（包括所有把网络策略设为 `off` 的人）完全无从进入。现在完整版中这个入口始终存在，来去的只有角标；审阅页则在 AppBar 上、以及作为空状态的主按钮，各带一个**检查更新**操作。这次检查走的是 `buildScanQueue` 构造的快照队列，在开始时即固定，因此进度条的分母永不变动、循环也一定会终止。它无视什么、尊重什么就是整个设计：失败退避与 30 天重新探索窗口被**无视**，因为「立刻重试」正是这个按钮的含义，而那两者只是「别太快重试」的启发式；用户的忽略被**尊重**，因为重新提议一次拒绝会让*忽略*失去意义；缓存新鲜度被**尊重**，因为对 200 条的库重新抓取仍新鲜的资料要花上几分钟、上千次请求却什么都不会改变；而 `metadataAutoUpdate` 策略被**无视**，因为该设置管的是*无人值守*的流量，这里则是用户可以看着、可以随时停止的一次明确点击——并且当策略为 `off` 时，这个按钮是唯一的检查途径。离线仍然拒绝执行，因为那样每一项都会失败、白白把整个库推进退避；为此需要把 `_isOffline` 从 `_networkGate` 中拆出来，后者一旦策略为 `off` 就会短路返回 `blocked`，而那恰恰是手动按钮必须能工作的场景。手动间隔比后台更紧，因为用户正在看着（刷新 2 秒、搜索 6 秒），并且仍受同一套算术约束：一次刷新对单一站点最多 1 次请求、一次 `searchAll` 最多 2 次，因此分别为每分钟每站点 ≤30 与 ≤20 次，而 Jikan 是 60、AniList 是 90。执行期间后台定时器会被取消——两个 worker 打同一批 API 会让速率变成这些间隔所设定值的两倍。队列为空时报告的是*所有资料都已是最新*而不是*未发现可用更新*：这是两个不同的结论，而当「没有检查任何东西」的原因是「没有东西需要检查」时，只有前者是真的。取消会让已发出的那次请求跑完，并保留已找到的结果。其次，**搜索对话框不再只显示一个光秃秃的转圈**。一次检索确实可能耗时约半分钟——每个来源各有 10–15 秒超时，且空手而归的来源会用首轮采集到的标题再查一次——在这段时间里，孤零零一个转圈与真正的卡死无法区分。`searchAll` 现在接受一个可选的 `onProgress` 回调，在每轮开始与每个来源作出回应时上报 `AnimeSearchProgress`，渲染为一条确定进度条外加每个来源一个 chip。每一轮保持**各自的分母**，因为第二轮只重查空手的来源，若用单一总计，分母会在检索途中变大、把进度条往回推。失败的来源计入「已回应」，但仍与「没找到」的来源保持可区分，因为对于正在决定要不要重搜的人来说这是两个不同的答案——`_runRound` 本就会吞掉各来源的异常以免一个失效来源拖垮整次检索，所以上报正是从同一个处理分支发出的。第三，`needsManualPick` 的建议新增了**手动搜索**操作，它会打开该记录的编辑页，搜索对话框已经展开且标题已填好，复用该页现有的搜索并应用路径，而不是新增第二条「用搜索结果写入记录」的通路。这要求先把审阅页正确地路由化：它此前是以命令式 `MaterialPageRoute` 推入的，而 Flutter 的 `Navigator` 会把基于 `pages` 的路由排在命令式推入的路由**下面**，因此从它内部发起的 `context.push` 会把编辑页打开在启动它的那个页面底下。现在它是一条 `/metadata-updates` 的 `GoRoute`，页面作用域通过 `extra` 传递。测试数 143 → 158。版本统一为 `1.5.1+54` / MSIX `1.5.1.0` / 安装包 `1.5.1`。
- `v1.5.2`：能用上宽屏的详情页，以及能用上别人分数的排行。**详情页的分栏是宽高比规则而非宽度断点，原因正是 Galaxy Z Fold 8。** 最显而易见的实现——在某个 `sw600dp` 式阈值之上分栏——恰恰在促成这项工作的那台设备上是错的：Fold 8 展开后是 4:3 的**横向**面板（2448 × 1848 px），因此竖屏时为 3:4，**比它所取代的近方形 Fold 7 更窄**（1848 px 对 1968 px），而 Fold 8 Ultra 却反向做到了 2256 px。如今一代产品展开后横跨约 672 到 954 逻辑像素，且同一台设备在同一宽度下需要两个不同答案：横屏双栏，竖屏保持原本的单栏。于是 `useDetailTwoPane` 检查三件事——宽度 ≥ 600、高度 ≥ 480、宽高比 ≥ 0.82——其中宽高比检查是承重的那一项，位于 Fold 8 竖屏 0.755 与 Fold 7 / Fold 8 Ultra 竖屏 0.90 之间空隙的中段。`kana_page.dart` 中已在使用的 720 阈值会直接让 Fold 8、Fold 5 和 Fold 6 落选；840 则只剩 Ultra。高度下限并非与宽度下限重复：仅凭宽高比检查会放行**又宽又矮**的视口，没有它，折叠状态的 Fold 8 封面屏旋转到横向（约 657 × 416 dp）和普通手机横屏（约 915 × 412 dp）都会被拆成两个逼仄的栏。由于该规则针对形状而非设备类别，4:3 和 16:10 的平板与 Fold 8 完全一致——横屏双栏，竖屏单栏。该判断读取 `MediaQuery.sizeOf` 而非用于给两栏定尺寸的 `LayoutBuilder` 约束，因为以 `Scaffold` body 度量会扣掉应用栏并抬高比值，使 Fold 8 竖屏读作 0.80，几乎不留阈值余量。页面被重构为四个构建函数——封面、头部、详情卡片、剧集列表——使两种布局共享每个区块的唯一一份代码，而头部与详情卡片之间的那道缝**就是**分栏边界。有两项封面定尺寸的限制是在看过渲染结果之后才加上的，而非事先推导：**半栏封顶**，因为在高 704 dp 时固定的头部预算会给封面 420 dp，两行标题加三排标签随即越过栏底；以及**宽度检查**，因为 3:4 面板相对宽度的纵向空间过于充裕，只按高度定尺寸会得到又高又窄的封面。其次，**排行视图现在可以按资料库评分而非你自己的评分排名。** 外部分数自 1.4.0 起就按来源存放在 `externalMeta.ratings` 中，而统计页从未读取过它们。承重的细节在于 `scoreMax` 是**逐条记录**存储的，因此任何跨来源比较都必须除回去——直接读 `score` 会把 bangumi.tv 的 7/10 排在 MyAnimeList 的 85/100 之上——于是有了评分上的 `normalizedScore`、元数据上的 `normalizedScoreFor` 与 `averageNormalizedScore`，并带 `scoreMax <= 0` 守卫，使格式错误的记录无法算出排到榜首的无穷大。跨来源平均是默认值，也可锁定单一来源；平均之所以是更稳健的默认，是因为一条记录恰好带有哪些来源因番而异，而锁定单一来源会静默剔除所有缺少该来源的动画。切换来源真正改变的是**哪些**动画出现，而不只是顺序——按自己的评分会剔除全部未评分的，按资料库会剔除全部从未抓取过的——因此过滤器与比较器被收拢到唯一的 `_rankingScoreOf` 缝合点，而不再是四处调用点，其中三处还带着默默依赖过滤器先跑过的 `!`。在资料库来源下，子分下拉被替换而非禁用，因为外部资料库只报告一个标量，"按画面排序"对它毫无意义。`ShareService` 无需改动签名：`sortLabel` 本就是普通 `String`。`/anime/ranking` 以增量且带默认值的 `scoreSource` 与 `externalSource` 参数获得了同一维度，并在 `sort` 中回显，因为 `buildRankingSnapshotForQuery` 的存在本就是为了让 API 与界面无法对排名的含义各执一词；带 `externalSource` 却不带 `scoreSource=external` 返回 400，而不是被静默忽略。测试数 158 → 186，其中包括一套渲染测试，以上文所列每台设备的真实逻辑像素几何驱动真正的详情页——Fold 8 的两种方向被断言为同一台设备给出两个答案——以及一套排行测试，其种子数据含一部仅由用户评分的动画和一部仅有资料库评分的动画，使得"切换却什么都没变"的实现无法通过。版本统一为 `1.5.2+55` / MSIX `1.5.2.0` / installer `1.5.2`。
- `v1.5.3`：终于能读全的长名称、会利用宽屏的列表，以及被从详情页提拔出来的拆分规则。**"何时拆分"这条规则本身是对的；错的是它待的地方。** 1.5.2 从 Galaxy Z Fold 8 推导出了它——宽 ≥ 600、高 ≥ 480、宽高比 ≥ 0.82，其中宽高比测试承重，因为 Fold 8 展开后是 4:3 的**横向**面板，因而必须横屏拆分、竖屏保持单栏——但它待在 `detail_layout.dart` 里、用着详情页的名字，也被当作详情页的规则来记录。现在它是 `shared/utils/adaptive_layout.dart` 中的 `canSplitLayout`，`useDetailTwoPane` 成了一个单行委托，测试断言两者仍然一致；它还拥有了自己的跨领域文档页 `doc/zh-cn/adaptive-layout.md`，以及 AGENTS.md 阅读索引表中的一行。一旦窗口可以拆分，三个数据浏览列表就会把它填满：在容得下的范围内容纳尽可能多的、不低于 320 dp 的列，上限四列，按从左到右、然后从上到下排布。列数遵循 Google 的 feed 规范——自适应最小条目宽度，而非为每个断点写死一个列数——而门控保留了宽高比测试，这是对 Google"用宽度而非宽高比"建议的**刻意分歧**，并被如实记录：单靠宽度无法让 Fold 8 在同一个宽度下给出两个不同的答案。三个模块各自把自己的列数记在 `storage_config.json` 中，默认自动；窗口变窄时该值是被钳制而非改写的，因此在桌面端设定的列数能挺过一次折叠并在展开时回来；清单文件本就已经声明了 `screenLayout|screenSize|smallestScreenSize|density`，所以这个转换完全不需要任何生命周期处理。管理页的滑动编辑与滑动删除在单列时原样保留，在多列时被去掉，因为在一个窄单元格内水平拖动的含义是含糊的——何况新的长按面板本来就承载了这两项操作。那个面板是本次发布的另一半：三个标签页的每一行都把标题截断为单行，因此长按（或者右键，毕竟在 Windows 上按住鼠标键不放并不是谁会做的手势）现在会完整展示每一个已存储的标题，可选中、可换行，下方是**编辑**与**删除**，其中删除复用共享的 `confirmDelete` 并继承其五分钟"不再询问"窗口。`kana_page.dart` 自己写死的 720 被**刻意保持原样**并记录为已知例外，因为让它走宽高比规则会改变它在平板竖屏下的行为，而没有人要求过这一点。测试从 186 项增加到 216 项。版本统一为 1.5.3+56 / MSIX 1.5.3.0 / installer 1.5.3。
- `v1.5.4`：假名页与设置页接入共享布局规则，导航在宽窗口上移到侧边。**侧边导航栏是牵动其余一切的那处改动。** 600 逻辑像素以上，外壳的底部 `NavigationBar` 变成侧边 `NavigationRail`——这既是 Google 自己的建议，也正是拆分规则刻意拒绝的那个情形：手机横持于 915 x 412 远不够高来承载两栏，却要把五分之一的高度花在底栏上，而 915 像素的宽度闲置着。因此 `useNavigationRail` 是**第二条、只看宽度的规则**，刻意与 `canSplitLayout` 分开，并如实记入文档。它把五个目的地居中（`groupAlignment: 0`）而非顶部对齐，因为它下面没有前导菜单按钮或 FAB 可以依托。有两个后果贯穿应用其余部分：所有容量现在都对 `shellContentWidth` 测量，它会减去导航栏的 81 像素；而滚动页面为底栏预留的 80 dp 在没有底栏时降为 16。这项修正是真实且可见的——**平板横持现在得到两列动画列表，而 1.5.3 给的是三列**，因为剩下的 943 若分三列则每列 306 宽，低于 320 这个最小值。变的是空间，不是规则。假名页在拆分规则允许**且**两张不低于 330 dp 的表放得下时变成两列——高的五十音表与拗音表在左，矮的浊音表与规则卡在右，假名切换移到搜索字段旁边而非其上方。第二道门控正是让较窄的展开态折叠屏保持单列而无需自己的断点的原因，而 Fold 8 Ultra 与 Pixel 10 Pro Fold 则放得下，每格约 58 像素，与手机持平。这同时**解除了 1.5.3 记录的那条已知例外**：假名页写死的 `constraints.maxWidth >= 720` 已经消失，接入了泛化后的 `columnCapacity`，而这样做**保住**了平板竖持的行为而非改变它——侧边导航栏使规则宽度只剩 655 dp，那个 720 字面量如今会在此落空，而共用的算式则通过。`lib/` 中不再有任何一个内联的宽度断点。设置页在拆分规则允许的任何窗口上变成列表-详情布局：一级列表留在左边，它所通向的二级页面——WebDAV 同步、备份、查重、隐私政策、许可证——填满右边而不是全屏推入，当前所在的行被高亮，未选中时显示占位提示。**这五个页面没有一个需要改动才能被嵌入**，因为详情栏把它们寄宿在一个只持有单条路由的嵌套 `Navigator` 中，它报告 `canPop == false`，于是它们的应用栏不会长出返回箭头。开源许可证页刻意保持全屏：它本身已包含自己的主从结构，嵌套它会让屏幕上出现三栏。合上设备会保留选择而非清除，与列数偏好被钳制而非改写是同一个道理。设置页的门控严格沿用 `canSplitLayout`，不加额外的宽度阈值——这是一个被接受的取舍，因为 Z Fold 5 展开后只剩 578 dp，那里的设置行确实很挤，换来的是一条规则而不是两条。设置页的下拉框现在齐右对齐，按钮面与菜单项两者皆然，因此菜单打开时标签不会横向跳动。顺带修复：在任何未初始化 `launchAtStartup` 的环境——也就是除真实应用入口之外的任何环境——打开设置页会抛出未处理的异步错误。测试从 216 增至 244。版本统一为 1.5.4+57 / MSIX 1.5.4.0 / installer 1.5.4。
- `v1.5.5`：番剧编辑页、统计摘要与排行筛选面板接入共享布局规则。**编辑页在 `canSplitLayout` 允许的任何窗口上拆成双栏**——封面选择器与两个标题字段固定在左侧，从季度往下的一切在右侧滚动——经由详情页所用的同一个 `useDetailTwoPane` 委托；左栏靠构造不滚动，新增的 `editCoverSize` 取用 `editLeftPaneFieldBudget` 之后剩下的高度，使那一列在每一个可拆分高度上都放得下，并以一个 `SingleChildScrollView` 守住软键盘把 body 压到该范围之外的情形。**统计摘要以 2 × 2 网格移到趋势图旁边**，由三个条件门控——`canSplitLayout`、新增的 `useStatsSideBySide`（260 dp 卡片栏加 380 dp 图表再加间距），以及趋势数据非空，因为空图表什么也不渲染、会让卡片孤零零地留在那里；那道宽度底线使 Z Fold 5、Z Fold 6 与 Z Fold 7 竖持保持堆叠布局，而 `statsSummaryPaneWidth` 不需要右侧上限，因为在门控之上栏宽按宽度的 0.34 增长而图表按 0.66 增长。**排行筛选面板**把控件两两配对：时间与类型从 572 dp 起经由在新增 `rankingFilterMinWidth` 上的 `columnCapacity` 共处一行，评分来源、排序依据与升降序从 674 起经由 `useRankingSortRow` 共处一行，把约 244 dp 的筛选外壳降到 124——两处都只看宽度，因此手机横持尽管从不拆分也能得到它们，而那正是这份节省最要紧的地方。**对 1.5.4 那句"`lib/` 中不再有第二条布局规则"的更正**：`statistics_page.dart` 仍为自定义区间按钮带着内联的 `constraints.maxWidth < 560`，之所以被漏掉，是因为得出那句结论的检索只覆盖了 1.5.4 改动过的页面；它现在走与筛选行相同的 `columnCapacity` 调用，把阈值移到 572，此外别无改变，把它并入之后那句结论便成立了。`_buildSummaryCard` 返回裸 `Card`，由两种卡片布局各自提供 `Expanded`；`_buildTrendChart` 新增 `padded` 开关供嵌入副本使用；`_buildRankingFilters` 把布局判定与装配拆开。测试从 244 增至 265，新增 `anime_edit_two_pane_ui_test.dart` 与 `statistics_layout_ui_test.dart`（两者都以简体中文驱动，因为 `flutter_test` 的默认字体把每个字形都渲染成一个全角方块，会让拉丁文标签在生产环境本来宽裕的宽度下溢出）；`local_archive_ui_test.dart` 现在经由 `find.byType` 指认表单的 `ListView`，而不再当作页面上的第一个 `Scrollable`。没有新增本地化键。版本统一为 1.5.5+58 / MSIX 1.5.5.0 / 安装包 1.5.5。
- `v1.5.6`：四处布局微调，外加一个完全没有布局规则的页面。**统计摘要现在相对趋势图上下居中**，而不再吊在那一行的顶端：2 × 2 网格高约 160 逻辑像素，图表块约 268，而由于网格是较矮的那个子控件、且是普通的 `SizedBox` 而非 `Expanded`，这一行的高度本来就是图表的高度，因此 `CrossAxisAlignment.center` 就是全部的修复——不需要 `IntrinsicHeight`，也不需要任何测量。**排行的排序行改由排序依据领头**，因此它的描边左缘与上一行的时间下拉框对齐，两个分段按钮成组靠右；1.5.5 由评分来源领头，把一个胶囊按钮放在了一条描边之下。`useRankingSortRow` 是一个和，对顺序视而不见，因此阈值仍是 674；这个顺序也刻意**不**带进堆叠形态——674 以下没有东西可以对齐，而把评分来源放到它所控制的下拉框之下，会让人先读到「按 X 排序」，才知道 X 取自哪里。**`metadata_updates_page.dart` 获得了列规则**，此前它一条也没有：没有 `LayoutBuilder`，没有 `MediaQuery`，也没有任何断点，因此仅有的一列提案卡被拉伸到窗口有多宽就多宽。它现在先走 `canSplitLayout`，再走在新常量 `metaUpdateCardMinWidth` 上的 `columnCapacity`（360——一张卡自身花 24 的内边距，每一行变更再花 24 + 8 + 84 给复选框、间距与字段标签，给旧值、箭头与新值留下约 220），于是折叠屏横持两列、桌面四列。有两点使它不同于其他多列页面：它被推到外壳**之外**，因此量的是原始窗口，绝不能使用 `shellContentWidth` 或 `shellListBottomInset`；以及它保留了 `ListView.builder`，因此用 `adaptiveTileRow` 而非 `adaptiveTileRows` 组行——后者会把每个 tile 都实例化，从而丢掉虚拟化。它没有列数偏好——为此新增第四个存储设置并不值得，因此与假名页一样只走自动。**后台更新策略下拉框从其设置 tile 的 `trailing` 位移到了标题行上**，并抽出为 `_buildMetaPolicyDropdown`：`trailing` 与标题**和**副标题共享同一条水平带，这把该行很长的说明困在一条又窄又参差的列里——在设置的双栏布局下折成六行——现在它整宽延伸到下拉框下方。标题外的 `Expanded` 使它在最窄的栏里换行而不是溢出；下拉框的 `style` 被钉在 `bodyMedium`，因为 `ListTile` 给 `title` 的字号大于 `trailing`。测试从 265 增至 274，新增 `metadata_updates_layout_ui_test.dart` 以及 `adaptive_layout_test.dart`、`statistics_layout_ui_test.dart` 与 `settings_two_pane_ui_test.dart` 中的用例；摘要行通过新增的 `ValueKey('statsSummaryPane')` 钉住。两处文档更正：`functions/INDEX.md` 仍把 `adaptive_layout.dart` 记为 `4 | 4`——那是它在 1.5.3、该模块尚未长成全应用策略时的计数，仅此一项就占了 10 个声明「已知缺口」中的 8 个；以及 `AGENTS.md` 的发布清单从未列出 `anime_search_service.dart` 的 `_userAgent` 字面量，那是一处靠运气一直保持正确的手工维护版本位置。没有新增本地化键。版本统一为 1.5.6+59 / MSIX 1.5.6.0 / 安装包 1.5.6。
- `v1.5.7`：anime1.me 观看链接查找围绕站点自己的系列索引重建，中文转换表改为生成，观看进度得以存储、在后台刷新并在三处显示。**搜索端点是用错了工具。** anime1.me 的 `?s=` 搜索只匹配繁体的精确子串，因此简体标题、日文标题或大陆译名一无所获，而 1.4.0 时代的代码用最多六次串行、每次十秒超时的请求与两字子串猜测来弥补。站点同时提供 `animelist.json`——首页表格所渲染的约 1,900 行数组（分类 id、标题、集数文本、年份、季节、字幕组）。新的 `Anime1Service` 每三十分钟下载它一次（并发调用共享请求；刷新失败时保留旧副本），把每一行对记录的标题、日文标题、英文与罗马音标题及已存别名打分，只在索引不可达或一无所获时才回退到旧的抓取。打分跑在**归一化的简体键**上——全角变半角、小写、去掉标点与符号、两侧都转成简体，即多对一的方向——因为 `toTraditional('弄干净')` 是 `弄幹淨`，永远等不上站点的 `弄乾淨`；同一归一化现在也支撑 `relevance`，只会抬高分数。行需要 0.5 的相似度**并且** 0.4 的顺序敏感分数，后者是因为基于集合的 Dice 项把 `bocchitherock` 对 `tomjerry` 恰好打到 0.5。记录的首播季度给对应季节的行加 0.10（9 月 29 日首播向前贴靠到秋季，与站点归档一致），从标题或季度标签读出的 第N季 / Season N 序数加 0.10 或减 0.05，因此标记为「第二季」的记录不再与标题精确相同的第一季打平。没有行达到 0.9 且没有已存中文别名时，一次 bangumi.tv 查询补采别名——其别名字段通常把台译列在大陆译名旁边——索引据此重新排序；对话框以一行说明告知。**`ChineseConvert` 曾是约 1,200 对的手打列表**，缺 干/乾/幹、髮、裏、臺、徵、迴 以及数百个其他字；`tool/gen_chinese_convert.dart` 现在从 OpenCC 的字符字典（Apache-2.0，在许可证页署名，输入钉在 `tool/data/` 中）与旧表合并生成，使任何繁体查询变体都不倒退，并以 O(1) 查找取代线性扫描。**进度现在是数据。** 选择一条结果会把站点的集数单元格存为 `externalMeta.watchProgress`（`AnimeWatchProgress`：读取时所用的 URL、分类 id、最新一集、原文、连载中标志、`checkedAt`）；详情页标签读作 `anime1：更新至第 9 集` 并可点按重新检查，首页在站点已列出该集时把该行的观看按钮变成主色，管理页在行末追加 `更新至 9`，本地 API 也会报告它。它住在 `externalMeta` 里，因为它就是那一类数据——经 `patchExternalMeta` 写入，绝不修改 `modifiedAt`——而 `validWatchProgress` 在链接一被改动时就把它隐藏。`MetadataUpdateService` 在刷新与探索之间多了第三步：一次索引下载覆盖库中每个 anime1 链接，因此整个到期集合在每个 tick 内处理——连载中每 6 小时、完结后每 7 天、已看完的完结作品永不——1.5.7 之前的 `/category/…` 链接以每个 tick 三次页面抓取节奏处理，失败走一小时的内存中重试，刻意与条目退避分开。四套目录各新增十个本地化键；隐私政策注明后台更新现在也会访问 anime1.me。测试从 274 增至 312，新增 `anime1_service_test.dart`、`chinese_convert_test.dart` 与 `watch_progress_test.dart`。版本统一为 1.5.7+60 / MSIX 1.5.7.0 / 安装包 1.5.7。
- `v1.6.0`：系列、分类与推荐，外加可选的端侧模型，五十音标签退为一个设置项。**五十音标签现已默认隐藏，已有安装同样如此**，因为假名练习已由独立的 MyNihongo!!!!! 应用承担；可在设置 › 通用 › 五十音速查中重新开启（`kanaTabEnabled`，设备本地），关闭时 `/kana` 重定向到主页。**系列关联取代了旧的上一季/下一季规则**——旧规则只匹配完全相同的 `displayTitle`，并把季度标签当字符串比较（因此 `Season 10` 排在 `Season 2` 之前）：确定性的 `SeriesIndex` 现在根据去掉季度标记后的标题、旧的同名规则以及资料库关联关系（AniList 按 id 查询的关联、Jikan `/full` 的关联，以及 bangumi.tv 的关联条目接口，每次刷新多一个请求）来归组各季；详情页显示*系列*卡片，支持手动整理——关联、移出（独立）、重新排序、交给应用判断、添加下一季——并对番剧库里缺少的续作显示「下一部：<标题>」提示。**新增的同步字段**在未使用时都会省略，因此旧记录的序列化结果不变：`seriesLink`（分享文件导出时去除、导入时丢弃）、`categories`（用户自己的分类；存在但为空是有意义的，会被写入）以及 `externalMeta.relations`；分享导入现在还会保留公开的 `externalMeta`，早先版本会丢弃它。**自动分类与推荐**均默认关闭，且不依赖任何模型即可工作：23 个 id 的分类体系（没有成人类分类）、来自三个资料库的类型标签映射、管理页的分类筛选，以及只从自己番剧库中推荐的「接下来看什么」页面——确定性排序，每个系列只有最早未看完的一部可以成为候选。**端侧 AI 为可选项，默认关闭，尚未在真机上验证**：Android 上通过 AICore 使用 Gemini Nano（`genai-prompt` 1.0.0-beta4、四种变体探测、R8 保留规则），iOS 与 macOS 26 上使用 Apple 的 Foundation Models（本地插件、弱链接并在 CI 中检查），用于填补分类空缺并撰写带标注的简短推荐理由；它产生的任何内容都不同步也不备份，只在应用位于前台时运行，从不使用云端模型，开关关闭时方法通道一次都不会被调用。**Android 现在需要 8.0（API 26）**，不再支持 7.0 与 7.1，因为 ML Kit GenAI 库如此要求。隐私政策新增端侧 AI 一节。2026-09-24 与用户确定的决定：把 `minSdk` 提高到 26，而不是在运行时对 API 24–25 做防护（有类校验崩溃的风险）；分类、推荐与端侧 AI 同样随商店版发布（它们自身不发起网络请求）；已有用户同样隐藏五十音标签；按提议采用分类体系（id 一经发布即固定）；分享文件保留 `categories`（不属于个人数据），而 `seriesLink` 始终去除；五十音开关的说明提到 MyNihongo!!!!!，但不附商店链接。测试从 312 项增加到 427 项。版本统一为 1.6.0+61 / MSIX 1.6.0.0 / 安装包 1.6.0。
