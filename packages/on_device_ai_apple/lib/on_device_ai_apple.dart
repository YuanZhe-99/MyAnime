/// MyAnime!!!!!'s bridge to Apple's Foundation Models framework.
///
/// This package exists only for its native side: on iOS and macOS it registers
/// the `com.yuanzhe.my_anime/genai` method channel, which the app's
/// `MethodChannelGenAiBackend` talks to. There is no Dart API here. See
/// `doc/en-us/on-device-ai.md` in the MyAnime repository.
library;
